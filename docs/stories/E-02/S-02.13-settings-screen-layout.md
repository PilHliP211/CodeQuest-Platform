# S-02.13: Settings Screen Layout

**Epic:** E-02
**Size:** XS
**Dependencies:** S-02.06

## Skills to Load

- `react-component-structure` — file template
- `naming-conventions` — `SettingsScreen.tsx` in `src/components/Profile/`

## What

The settings screen shell — heading, sections, back button. Doesn't actually edit anything yet (S-02.14) and isn't navigable yet (S-02.15).

## Tasks

1. Create `src/components/Profile/SettingsScreen.tsx`:
   ```tsx
   interface SettingsScreenProps {
     onClose: () => void;
   }

   export function SettingsScreen({ onClose }: SettingsScreenProps): React.ReactElement {
     return (
       <div className="...">
         <header>
           <h1 className="font-pixel">Settings</h1>
           <button type="button" onClick={onClose}>Back</button>
         </header>
         <section aria-labelledby="profile-section">
           <h2 id="profile-section">Profile</h2>
           {/* Name change form added in S-02.14 */}
         </section>
         {/* Reset Progress placeholder — implemented in E-12 */}
       </div>
     );
   }
   ```
2. Use semantic `<section>` with labeled headings.
3. The "Reset Progress" button is NOT implemented here — leave a comment.

## Done When

- [ ] `SettingsScreen` exists with heading, profile section, back button
- [ ] Back button calls `onClose` prop
- [ ] Comment placeholder for "Reset Progress" (deferred to E-12)
- [ ] Uses pixel font for headings
- [ ] No interactive elements yet besides the back button
